module.exports = {
  theme: {
    extend: {
      fontFamily: {
        handwriting: ['"Just Another Hand"', 'cursive'],
      },
      keyframes: {
        shimmer: {
          '100%': { transform: 'translateX(100%)' },
        },
      },
    },
  },
};
